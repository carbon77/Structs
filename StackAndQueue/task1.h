#pragma once

#include <stack>
#include <string>
#include <map>
#include <iostream>
#include <cmath>

void testTask1();
std::string fromInfToPost(std::string& exp);
std::string fromPostToInf(std::string& exp);